"""
convgeno — Reusable utilities for the gene family copy number pipeline.

Subpackages:
    io          FASTA parsing, table I/O, path utilities
    validation  Input validation for proteomes, trees
    external    Wrappers for CLI tools (OrthoFinder, CAFE-5, BadiRate)
    manifests   Checksum generation, run metadata recording
    utils       Command runner, logging, config loading
"""
